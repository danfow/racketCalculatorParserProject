I've augmented the grammar with an explicit end of input marker '$$', mostly so I can add comments to the file. Your program should stop reading (or at least stop parsing) once the $$ token is encountered. 
